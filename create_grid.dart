import 'dart:math';

class grid {
    List< List<int> > grid = [];
    Random rand = Random();
    for () {
        List<int> row = [];
        for (int j = 0; j < 10; j++) {
            row.add(rand.nextInt(10) + 1);
        }
        grid.add(row);
    }

    createGrid() {

    }

    cityPopulate() {
        
    }
}